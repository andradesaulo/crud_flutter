import 'package:flutter/material.dart';
import 'package:crud_flutter/ui/listview_personagem.dart';

void main() => runApp(
  MaterialApp(
    title: 'Carregando dados',
    home: ListViewPersonagem(),
  )
);